<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  text-align: left;
  padding: 8px;
}

tr:nth-child(even){background-color: #f2f2f2}

th {
  background-color: #4CAF50;
  color: white;
}
</style>
  </head>
  <body>
    <h1>Hello <%= @startup_application.startup_name%></h1>
	<p>
		Contract!! You have successfully entered to venture development courses.Please check below to view list of assigned courses and ensure to complete within target date.
	</p>
  <div>
      <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Activities</th>
          <th>target_date</th>
      </tr>
    </thead>
    <tbody>
      <% @courses.each do |course| %>
      <tr>
        <td><%= raw(course.title)%></td>
        <% course.activities.each do |activity| %>
           <td><%= raw(activity.name)%></td>
        <% end %>
         <td><%= course.target_date%></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  </div>
  </body>
</html>