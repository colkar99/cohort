<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  text-align: left;
  padding: 8px;
}

tr:nth-child(even){background-color: #f2f2f2}

th {
  background-color: #4CAF50;
  color: white;
}
</style>
  </head>
  <body>
    <h1>Hello <%= @startup_user.full_name%>%></h1>,
	<p>
		Admin has reponsed checklists given below,
	</p>
  <div>
      <table>
    <thead>
      <tr>
        <th>Program title</th>
        <th>Startup name</th>
          <th>Course name</th>
          <th>Checklist names</th>
          <th>Checklist status</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%=raw @program.title %></td>
          <td><%= @startup_profile.startup_name %> </td>
          <td><%=raw @course.title%></td>
          <% @checklists.each do |checklist| %>
            <td><%=raw checklist.name %></td>
            <% if checklist.is_passed %>
              <td style="color: green">Passed</td>
            <% else %>
              <td style="color: red">Not Passed</td>
            <% end %>
          <% end %>
        </tr>
  
    </tbody>
  </table>
  </div>
  </body>
</html>